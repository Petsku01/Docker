# Crawler Configuration Example
# Copy this file to .env and configure for your environment

# URLs to crawl (comma-separated)
START_URLS=https://example.com,https://example.org

# Rate limiting (seconds between requests)
RATE_LIMIT_SEC=5

# Output file path
OUTPUT_FILE=/app/output/crawled_urls.csv

# Proxy URL (optional, leave empty if not using proxy)
# Examples: http://proxy.example.com:8080, socks5://proxy.example.com:1080
PROXY_URL=

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
